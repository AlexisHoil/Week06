#include <stdio.h>

int perimeter;
int area;

int main(void) {

perimeter= (2*3)+(2*5);
area= 3*5;

  printf("the perimeter is %d in\n", perimeter);
  printf("the area is %d in\n", area);
 
  return 0;
}
